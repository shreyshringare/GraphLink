name: GraphLink CI

on:
  push:
  pull_request:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - run: echo "Build C++"

      - run: echo "Run Backend"

      - run: echo "Run Frontend"
